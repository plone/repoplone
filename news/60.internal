Standardized the release step function contract: all step functions now accept `**kwargs` and `PipelineReleaseStep` carries a `kwargs` mapping that the pipeline forwards. Preparatory refactor for configurable release pipelines. @ericof
